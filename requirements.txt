# --- UI ---
streamlit>=1.30

# --- LLM (Gemini SDK + LangChain integration) ---
google-genai>=1.0
langchain>=0.3
langchain-core>=0.3
langchain-text-splitters>=0.3
langchain-community>=0.3

# --- RAG: embeddings + vector store ---
chromadb>=0.5
langchain-chroma>=0.1
langchain-huggingface>=0.1
sentence-transformers>=2.7

# --- AI fallback ---
groq>=0.9

# --- Utilities ---
python-dotenv>=1.0
requests>=2.31
beautifulsoup4>=4.12
lxml>=5.0

# --- PDF support (syllabus ingestion) ---
pypdf>=4.0

# --- Optional: full syllabus scraping from GGC SimpleSyllabus ---
# Uncomment + run `playwright install chromium` to enable full-text fetch.
# playwright>=1.40
